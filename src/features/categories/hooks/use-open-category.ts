import { createOpenStore } from "@/lib/create-store";

/**
 * A global state hook managing the visibility and tracking state of the category sheet.
 */
export const useOpenCategory = createOpenStore();
